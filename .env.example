# Application base URL - update this when your tunnel/domain changes
APP_URL=https://your-tunnel.devtunnels.ms

# Cookie secret for session security (change in production)
COOKIE_SECRET=change-this-in-production

# Redis URL for production (optional - uses in-memory storage if not set)
# REDIS_URL=redis://localhost:6379
